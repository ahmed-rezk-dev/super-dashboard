/*
 * Metric Messages
 *
 * This contains all the text for the Metric component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Metric';

export default defineMessages({
	header: {
		id: `${scope}.header`,
		defaultMessage: 'This is the Metric component!',
	},
});
