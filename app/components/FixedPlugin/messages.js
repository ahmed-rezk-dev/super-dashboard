/*
 * FixedPlugin Messages
 *
 * This contains all the text for the FixedPlugin component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FixedPlugin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FixedPlugin component!',
  },
});
