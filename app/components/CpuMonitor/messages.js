/*
 * CpuMonitor Messages
 *
 * This contains all the text for the CpuMonitor component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CpuMonitor';

export default defineMessages({
	header: {
		id: `${scope}.header`,
		defaultMessage: 'This is the CpuMonitor component!',
	},
});
