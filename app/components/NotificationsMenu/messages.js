/*
 * NotificationsMenu Messages
 *
 * This contains all the text for the NotificationsMenu component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NotificationsMenu';

export default defineMessages({
	notification: {
		id: `${scope}.notification`,
		defaultMessage: 'Notifications',
	},
});
