/*
 * CustomMap Messages
 *
 * This contains all the text for the CustomMap component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomMap';

export default defineMessages({
	header: {
		id: `${scope}.header`,
		defaultMessage: 'This is the CustomMap component!',
	},
});
